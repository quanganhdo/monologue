%h2 Latest Updates

%ul 
  - for post in @posts
    %li
      %h4
        %span.meta
          = "Posted on #{post.created_at.strftime('%A, %B %d %Y at %I:%M%p')}"
          %a{:href => "/#{post.id}"} #
      .postcontent
        %p= post.content
      .bottom_of_entry